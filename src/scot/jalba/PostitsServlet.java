package scot.jalba;

import javax.servlet.annotation.WebServlet;

@WebServlet(urlPatterns = "/marketplace")
public class PostitsServlet extends javax.servlet.http.HttpServlet {

    // looks like for now we don't have anything to do here
}
